# 待 Alex 决策（DECISIONS-NEEDED）

格式：`## D<编号> · 日期 · [待决 | 已重基准-待复核 | 已决]`，正文写背景 / 选项 / 循环的建议。
Alex 决策后把结论写在条目下，状态改 `已决`——循环每轮开工会扫一眼本文件，见到新的 `已决` 会解除对应 BLOCKED。

## D1 · 2026-07-19 · [待决] golden_3d.json 本体是否入库？

**背景**：golden_3d.json（3D golden 场的位同基线）历来不入库——/check 的工作流语义是
"改动前本地捕获基线"。但它只有 **2037 字节纯文本 JSON**，且 HANDOFF §9 批评过
"golden 数字零版本记录"。P0.4 已入库 `golden_3d.meta.json` 侧车（sha256 + 环境 + 认证记录），
新 clone 可用侧车校验本地 golden 是否与认证版本一致，但仍无法凭 clone 直接 --check。

**选项**：
- **(a) 入库本体（循环的建议）**：2 KB 文本，diff 友好；clone 即可 --check；重基准时
  json + meta 同 commit 更新，版本史完整。/check 文档的"先捕获"语义改写为"以入库版为基线"。
- **(b) 维持不入库**：保留"本地捕获"工作流不动，靠 meta 侧车校验。保守，无文档改动。

**循环的建议**：(a)。2 KB 的入库成本可忽略，收益是 golden 版本史与代码史合流（正是 §9 的诉求）。
你拍板后：a → 循环在下一轮把 json 入库并刷新 /check 文档；b → 维持现状，条目关闭。

**决策（Alex，2026-07-19）**：a，入库。
**执行（iter 5）**：golden_3d.json 入库、meta 侧车 history_note 同步、/check §2 工作流改写为
"入库版即权威基线；重基准 = json+meta 同 commit 带 `!`"；2D 无入库基线，仍走本地捕获流程。

**状态**：已决
