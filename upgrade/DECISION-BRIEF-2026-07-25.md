# 决策简报 · 2026-07-25（iter 80）

给 Alex 的一页纸。背景全文在 `DECISIONS-NEEDED.md`（12 条）与
`docs/DF-CALIBRATION-AUDIT-2026-07.md`（§8–§15）；这里只放**你要拍的东西**、
**决定它的那一个数**、以及**拍了之后解锁什么**。

**当前状态**：候选 D 的 D-0 / D-2a / D-2b 主体 / D-2c / D-2b-5a / D-2b-5c 全部收案。
**已无不被阻塞的实质条目**——五条待决全部需要你或数据方答复。

---

## 按杠杆排序

### ① D10 · 产品交付按哪种进出口接法？ ★最高杠杆

**一个数**：同一台样机、同一组 16 工况（流量逐位相同）、相隔一周、进出口调换，
实测空气 Δp 比 = **中位 1.274**。

**为什么要紧**：生产 γ_df 的 534.8 对应 20260401 原接法，双层合成面锚在
20260407 调换后接法。**两者各自都对，描述的是不同管路**。选错方向 =
系统性 27% 的 Δp 偏差。

**解锁**：D-3 换默认（现 BLOCKED）、D-2c' 方法对比矩阵的打分口径。

**为什么先问它**：这是**产品事实，不需要任何新实验**——只要知道交付件怎么接管。

**循环的建议**：先答这条。若两种都可能，就按 (c) 让闭合显式带流向维度、
默认取保守（高）方向。

---

### ② D8 · 7-6 样机水侧流通面积是多少？

**一个数**：水侧 γ_HX 是气侧的 **×2.27 / ×1.77**（D/G）。反解需要
A_water = 0.627×/0.736× A_air 才能对齐；表内标称通道比 34 气/28 水（=0.824×）
只吃掉一半。

**为什么要紧**：气侧工具按"双流对称芯两侧同几何"把 A_flow 推广给了水侧，
**这条从未经表证实**。

**解锁**：D-2b-4w 双层合成面水侧腿。

**循环的建议**：只要一个数——对应 D-7-6 工作簿"流通截面积"列的**水侧**值。

---

### ③ D9 · sCO2 压差测点在哪、ṁ 是整机还是单侧？

**一个数**：同一芯、同一 A/L/Dh 口径、同一 K，反演出的 cF 是
空气 430/401、sCO2 **1522/1512**（1/m）——**×3.5/×3.8**。

**为什么这条比 D8 硬**：气与 sCO2 用**同一个** A_flow，而 cF ∝ A²，所以
**A 即使整体错了也在比值里精确约掉**。面积口径、长度口径、Re 外推、光滑基
不匹配、K 的选择——**全部已排除**。

**收口只需单一量级修正**：sCO2 的 Δp 实际低 72%、或 ṁ 实际高 88%、
或气侧 Δp 实际高 254%。不需要新物理。

**解锁**：D-2b-5b（γ_f 并入双层合成面）。在答复前 γ_f 冻结常数照旧不动
（它在自己口径内自洽、LOO 表现好），但**不能被解释成表面粗糙度物理**。

---

### ④ D12 · "边界效应系数" α 的推导是什么？

**一个数**：α ∈ [0.374, 0.608]，随 t 单调降；它贡献了 γ_spec 的 t 斜率的
**73%**（Diamond）；Gyroid 的 γ_spec **t 平坦是 α 相消出来的**
（α −0.720 抵掉原始 +0.720），L6 锚的 t 走向因 α 变号。

**为什么要紧**：α 的出处是工作簿一张 sheet，**仓库内无任何推导记录**，
而 γ_spec 的 (L,t) **外推形状**主要由它驱动。

**不阻塞什么**：已完成的结论**全部不受影响**（§12 水平 / §13 / §14——
理由：HX 层吸收水平偏差，且那些结论要么在固定几何要么是 data-vs-data）。

**阻塞什么**：把双层面用于 **7/0.6 以外几何**的寻优或换默认——
那里的真实不确定度大于现报的带宽。

---

### ⑤ D11 · 新几何（尤其 Gyroid L=7/8 列）CFD 质量与旧批一致吗？

**一个数**：sCO2 f 侧基重建后点级 RMSRE Gyroid 7.1%→**7.80%**（Diamond 8.9%→8.86%）。

**性质**：这是**确认题不是决策题**。重基准已落地且自洽——生产真正消费的
`γ_f·cF_smooth(7,0.6)` 全窗变化 ≤0.02%，且 (7,0.6) 从域外外推变成了插值节点。
G 的 RMSRE 上升更像是 5 个新几何加宽了拟合任务，而非数据质量问题。

**优先级最低**：不答也不影响任何事，只是留个复核痕迹。

---

## 如果一条都不答

循环没有可做的实质工作了。继续自动跑只会产出没有依据的数字——这比停下来更糟。

**建议转按需模式**（ROADMAP 末条）：拆定时器、改由你手动触发，
`PROTOCOL.md` / `STATE.md` / `ROADMAP.md` 原地保留，任何时候说一句
"继续升级循环"即可复活，磁盘状态齐全不丢进度。

**循环不擅自拆自己的触发器**——PROTOCOL §8 只授权"无任务或过期时重建"，
停跑是你的决定。定时器 `5e52a278`（`7,22,37,52`）仍在。

---

## 已完成的部分（供判断"值不值得继续投"）

候选 D 从 iter 58 起共 22 轮，产出 9 个诊断工具、审计 §8–§15 八节、
5 条重基准/决策登记。**推翻过 3 个自己先前的结论**：
"γ_HX ×1.26 拓扑无关"（iter 73）、"Diamond 双层闭合"（iter 73）、
"γ_HX 是纯拓扑常数"（iter 74）。最硬的两条正面结论是：
上海盲考钉死了 534.8 的出身（iter 77），以及三流体 cF 反演排除了面积口径
这个最省的解释（iter 76）。
