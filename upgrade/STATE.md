# 循环状态（STATE）

- iteration: 85
- 本轮（85）：**Gyroid sCO2 CFD 补全 L=8 列 → f 侧基重建 + γ_f/γ_Nu 重冻
  （§5 数值重基准）**。触发不是路线图而是新数据（Alex 2026-07-26 告知）。
  **纯增量已核**：旧 4400 行逐位保留（3e-16 ULP 级），新增 1000 行全在 L=8
  ——`G_8_3` 80→270（此前仅 7% 采样）、`G_8_4/5/6` 0→270（**此前完全缺失**）。
  几何 17→20。**Diamond 全程对照组**：预制表 max|ΔB|=0.000e+00、两组 γ 常数
  逐位重导相同 ⇒ 凡动了的都动在 Gyroid。两条 base-swap 绊线按设计只在 Gyroid
  炸响，**重导不放宽**。**抵消结构复现**：m/B 互抵 ⇒ 生产消费的 cF 全窗
  16 个未变几何 ≤0.64%、(7,0.6) −0.20…+0.62%，γ_f 乘积全窗 **0.008%**；
  真变化只有 G_8_3 的 +4.2…+5.1%。**实质发现：D11 待复核①结案**——Nu 的
  LOGO Gyroid medAPE **8.4%→7.9%**（几何还多了 3 个）⇒ iter 78 记的
  "RMSRE 上升是任务变宽非质量问题"得证，且钉出位置=L=8 列当时残缺。
  全 V&V：套件 1306+3skip+10、金门裸跑 PASS 位同、上海 3D 4.88%/2.11% GATE
  PASS、lumped 1.76% —— 空气/水面零影响。**§2 口径坑复现并按协议三方对照**
  （干净 HEAD 裸跑与本轮逐字节相同 ⇒ 已提交 CSV 生成于套件线程钉口径，
  已还原）。D13 登记、台账 SCO2-F-REFIT-0726。
- 本轮（84）：**收尾——终审报告补候选 D 章 + 转按需模式**。
  按 ROADMAP 末条章程执行：候选 D 可做部分穷尽（D-2c' 四变体全建成）、
  剩余条目全部等 D8-D12 人工答复、连报三轮无回应 ⇒ **拆定时器**
  （job `5e52a278` 已 CronDelete）。FINAL-REPORT.md 补章记下候选 D 的
  三条硬结论、三条被自己推翻的旧结论、五张表查了四张有病、以及交付物清单。
- 上轮（83）：**D-2c' 末项 = 贝叶斯标定变体（`fd97d7c`）**——问的不是点估计
  准不准，而是**D-2b-4 那条后验带面对完全外部数据集诚不诚实**（上海 16 从未
  参与其构造）。用 Δp 对 cF 单调这一性质，在 γ 后验五分位上跑门即**精确**得到
  Δp 同阶分位（5 次解代替 MC 上万次）。**结果**：dir(0401) 68/95% 覆盖
  **81%/94%**（15/16，几乎精确）；raw(0407) **0%/6%**。⇒ **同一条带，加了流向
  因子就诚实，不加则是"自信地错"**。原因：带只含 HX 案间散差、**不含流向这项
  系统不确定度**，也不含 §15 的 alpha 出处风险。**对 D-3 的含义：闭合的带必须
  带流向项，或配置必须钉死（D10）**。审计 §16.4。**D-2c' 四变体全建成。**
- 上轮（82）：**D-2c' 补建物理化 Ergun 族变体（`46310e5`）**——化到间隙流速
  约定后 ε 解析约掉，得 cF=1.75/(1.5·D_h)、K=0.015·D_h²，是个**零参数**闭合。
  五变体完整记分：production 4.88% / two_layer_dir 4.92% / two_layer 25.29% /
  **ergun 41.12%** / smooth_only 67.49%。**反直觉结果：ergun 优于 smooth_only**
  ——光滑基缺的粗糙度/HX 因子（~3.2x）比 Ergun 相对光滑基的高估（~2.0x）更大，
  即"量级对的粗糙经验式"胜过"缺一层物理的精确 CFD"，这正是双层架构的理由。
  口径未藏：A_0·D_h/(4·ε/2)=1.000 坐实单侧约定，两种 d_p 换算差 ~2 倍
  （cF 339.3 vs 169.6），实测气侧 ~400 正好被夹住。审计 §16.3。
- 上轮（81）：**D-2c' 方法对比矩阵（`66b23dc`）**——**纠正了上一轮的过度保守**：
  D-2c' 被 D10 阻塞的只是"裁决"不是"打分台"，而且两种流向都算就能把待决
  问题变成预先算好的表。记分：production 4.88% / two_layer 25.29% /
  **two_layer_dir 4.92%**（cF 526.54 vs 534.80，max|e| 14.92% 反优于 16.01%）/
  smooth_only 67.49%。**核心：534.8 基本可被预测出来**（零上海标定链 + 流向
  因子 = 与含标定生产面差 0.04 个百分点）；smooth_only 下界给出双层架构价值
  = 约 13 倍误差改善。**诚实性已写死**：two_layer_dir 非纯盲考（流向因子取自
  0401 即考卷本身），与 production 同为 1 个上海标量、信息量相同，差别在结构
  来源。审计 §16。
- 上轮（80）：**记账校正 + 决策简报**（无实质条目可做，按 §1.6 不空转）——
  ①ROADMAP 记账滞后校正：D-2sc（iter 61 已做、D6 已决）与 D-2a（iter 72=R2）
  补勾，D-2b 改标 `[~]` 主体完成（余 4w/5b BLOCKED）；
  ②新增 `upgrade/DECISION-BRIEF-2026-07-25.md`——给 Alex 的一页纸：五条待决
  按**杠杆排序**（D10 > D8 > D9 > D12 > D11），每条只给"要拍什么/决定它的
  那一个数/解锁什么"，并写明一条都不答时的建议（转按需模式）。
  DECISIONS-NEEDED.md 是参考文档不是决策工具，简报补的是这个缺口。
- 上轮（79）：**D-0 col47 试件锚溯源审计（`0dd90af`）**——主线被 D8-D11
  阻塞期间做地基。提取式钉到 file:line：**闭式反演不穿求解器**、**水平口径
  入锚**（出口恒钉 1atm，入锚 dP/P_atm 最大 0.815 ⇒ 承重，即 C8/打靶那条
  偏置的机理）。缺陷筛：**试件台账是唯一一张两类缺陷都为零的表**。
  **实质发现**：alpha（边界效应系数，~0.5 系统性乘子）**主导 gamma_spec 的
  几何形状**——D 占 t 斜率 73%，G 的 t 平坦是 alpha 的 −0.720 抵掉原始 +0.720
  相消出来的、L6 走向因 alpha 变号。**水平无害**（HX 层吸收，§12/§13/§14 结论
  全不受影响）、**形状有害**（§12.2 的带不覆盖 alpha 出处风险，离开 7/0.6 的
  外推真实不确定度更大）⇒ **DECISIONS D12**。审计 §15。
- 上轮（78）：**D-2b-5c sCO2 f 侧重提 + γ_f 重冻（`16a10ef`，§5 重基准）**——
  预制表重建至 07-23 修正导出（几何 D 15→20 / G 12→17，例数 7000→9799，
  B 中位 −10.6%，pooled m 各降 ~5%，K/ε_f/Dh 不变）；base-swap 绊线按设计
  炸响，`GAMMA_F_HOT` 四常数**重导而非放宽容差**。**关键**：dexp 降幅与
  pooled m 降幅几乎精确抵消 ⇒ 生产消费的 γ_f·cF_smooth(7,0.6) **全窗变化
  ≤0.02%**；真变化在其它几何（Re=2e4 光滑基 −8…−13%，= 新 CFD 的信息）。
  **(7,0.6) 样机几何从域外 RBF 外推变成插值节点**。全 V&V：套件 1306+3skip+10、
  金门裸调用 PASS 位同、上海 3D 16/16 dP 4.88%/Q 2.11% GATE PASS、
  lumped Q_air 1.76% —— 空气/水认证面零影响。D11 登记、台账 SCO2-F-REFIT-0725。
  **副产物**：金门口径坑写进 PROTOCOL §2（金门必须裸跑，套件的线程钉会造
  39 条 ULP 级假 FAIL；三步对照实测证明改动无关）。
- 上轮（77）：**D-2c 上海 16 例真盲考（`4adaddc`）**——候选 D 边界②兑现。
  双层合成面（零上海输入）盲预测上海 16：**16/16 全例偏低、偏置 −24.7%、
  RMSRE 25.3%**（生产 4.88%），而 **Q 几乎不动**（2.10% vs 2.11%）⇒ 纯阻力。
  裁决 **(ii) 成立**，且钉到具体原因：考卷（20260401）与 γ_HX 锚
  （20260407 **调换进出口**）是**同一台样机、同一组 16 工况（流量逐位相同）、
  相隔一周、进出口调换**；实测 Δp 比中位 **1.274**，与盲考因子 1.328、
  iter 73 残差 ×1.296 **三数同源** ⇒ **γ_HX 依赖流向**，两个锚各自都对，
  描述的是不同管路接法。⇒ **DECISIONS D10（产品交付按哪种接法？）**。
  审计 §14；§11 加补充（气侧 G/D 序再添"流向"这个混杂项，结论 1 不变）。
- 上轮（76）：**D-2b-5a 三流体 cF 反演对照（`983822a`）**——新增
  `cf_cross_fluid.py`。前置照 STATE 指示先查 sCO2 表：**无重复行**，冷侧确在
  仪表地板但 ok_dp 已挡且 D6 早选热侧，**热侧干净** ⇒ γ_f hot 不需重做。
  正题：γ_f 与 γ_total 挂在**不同光滑基**上，历史那句"超额 ×4.0-4.6"混了
  "基不匹配"与"数据打架"。剥掉闭合层只借一个 K 直接反演 cF：
  air 429.8/400.5、water 1339.5/829.3、sCO2 1522.1/1511.6（1/m，D/G）；
  **sCO2/气 ×3.54/×3.77**。**关键：A 不变**——气与 sCO2 同 A_flow 且 cF ∝ A²，
  比值里精确约掉 ⇒ D8 面积解释对本腿**不适用**；Re 外推、光滑基不匹配、
  K 选择也逐一排除。自洽核：气侧反演与 §12 的 γ_total×cF_dev 吻合 1-3%。
  ⇒ **DECISIONS D9**（问 sCO2 压差测点/ṁ 口径/是否同一件芯）。审计 §13。
  另：sco2_gamma_f docstring 就地更正过时的 ×4.0-4.6（仅 docstring，
  targeted 21 passed）；PROTOCOL §2 记入 GBK 控制台坑。
- 上轮（75）：**D-2b-4 双层合成面 + UQ 带（气侧腿，`3981f0f`）**——
  新增 `gamma_two_layer.py`：γ_total = γ_spec(L,t) × γ_HX(topo)，逐案 Δp 带
  （γ_HX 层 LOO）+ 面外推带。**UQ 结构改了**：两层强反相关（γ_HX 是拿 γ_spec
  点估计除出来的），独立抽样相乘会重复计入 γ_spec 的绝对水平 → 正确分解 =
  HX 实测钉住标定几何上的水平、γ_spec 只提供形状。**LOO 裁决 per_topo 双赢**
  （medAPE 3.5% vs pooled 5.9%、带宽 1.22 vs 1.49、68% 覆盖 70%/73%）⇒
  **气侧内部**用 per_topo（γ_HX D 1.078 / G 1.228），但带是"气侧内"带，
  不得宣称覆盖水/sCO2。**顺带修数据**：气侧两表也各有仪表地板案
  （D 工况1 γ=0.69 / G 工况1 γ=0.33）+ D 表 工况10/11 重复行（与水表同址，
  互相坐实）；修后中位不动（D 1.08 / G 1.23→1.24），σln 塌缩
  0.126→0.032 / 0.339→0.053 —— 此前的**带**是虚胖的。审计 §12，§11 加勘误。
  另修：df_refit 6 工具里 4 个在 GBK 控制台跑到记分板就崩（CSV 已写出所以
  看着像成功、实际 exit 1），§4 独立工具行的证据此前站不住；现 7 个全绿。
- 上轮（74）：**D-2b-3 γ_HX 水侧 + 气/水跨流体对照（`7ea25ec`）**——
  新增 `gamma_hx_water.py`。水侧 γ_HX D **2.44** / G **2.18**（vs 气侧
  D 1.08 / G 1.23）。两条结论（审计 §11）：①**G/D 序反号**（水 0.89 / 气
  1.15）⇒ γ_HX 不是纯拓扑常数，iter 73 那对数不可当拓扑参数写进合成面；
  ②水/气 ×2 的首要嫌疑是**水侧 A_flow 口径**（表内通道数 34 气 / 28 水，
  气侧工具按"对称芯同几何"推广给了水侧）——反解 A_water = 0.626×/0.733×
  A_air，标称 28/34 只吃掉一半 ⇒ **DECISIONS D8 待数据方**。
  顺带检出原始表两处缺陷：G 工况1 Δp = −48.4 Pa（负压差）、D 工况10/11
  除 ṁ 外逐位相同（复制粘贴）。
- iteration 73 ——**R1-R3 三轮批（Alex 2026-07-23 解除暂停指示"提交 master +
  恢复循环做 R1-R3"，2026-07-25 会话内连续完成）**：
  - R1 `54c9ed2`+`bc0f6da`：master `7ebdf6e`（水/sCO2 Nu 重拟至修正 CFD 上传，
    循环受托提交）合入 + γ_Nu 重冻 D 1.807/G 1.125（G 混杂解除，纯粗糙度因子）+
    金门 water_b 重采（air_air 位同）+ 数据底座同步。台账 NU-REFIT-0723、D7。
  - R2 `8190ccc`：dev 表切修正 Um 基，u 不变量 + (u_o/u_n)² 双重验证，
    LOO 反而更优（D cF 12.5→9.6%）。
  - R3 `d7d9a1d`：γ_spec/γ_HX 重跑三结论重验——**"×1.26 拓扑无关"推翻**
    （D 1.08/G 1.23 分化）、Diamond 双层改写为"试件面基本单独闭合"、
    G 对上海残差 ×1.296 量级确认（假说与 D-2c 盲考裁决法不变）。audit §10。
- in_progress: 无（iter 85 已收）
- **未决提醒（会话内发现，尚未立条目）**：`df_cfd_coeffs_dev.csv`（**气侧 dev
  面，不由 sCO2 数据喂养**，与 iter 85 不是同一条腿）上的试件台异常——
  把气侧试件实测拟成 `cF_exp = γ_core·cF_CFD + rig` 时，
  **Gyroid L∈{6,8} 解出 rig = −503（负台架损失，不物理）**，
  Diamond 同拟合给 γ=2.83 / rig≈0，而 7/0.6 HX 两点联立给 γ=1.38 / rig=170
  ——三者互相矛盾 ⇒ **芯体/管路分解欠定**。另：气侧试件 24 个几何里
  **12 个（L=4/5 全部）实测倍数 <1，即实测压降低于光滑壁 CFD，物理不可能**
  （`gamma_specimen.py` 的 `_TRUSTED_L` 早已硬编码只取 L∈{6,8}，
  但从未追查过原因）。**零成本可查，不需要新实验**，值得立为下一条目。
- next: **循环已停（按需模式）**。复活方法：在 worktree 目录说一句
  "继续升级循环"——本文件 + PROTOCOL.md + ROADMAP.md 齐全，不丢进度。
  复活后第一件事：查 DECISIONS-NEEDED.md 有没有新的 `已决`；
  若 D8-D12 仍全空，则仍无实质条目可做（见 DECISION-BRIEF）。
- 候选 D 边界（Alex 2026-07-22 四点拍板，原文见 PROGRESS iter58 段）：
  ①全串行 a→b 一次批到位；②CFD 拟合 (K,cF)、试件实验标定 γ，**上海 16 例退出
  标定转纯盲考卷**；③sCO2 γ 并入但排空气侧重锚之后；④UQ 要（γ 后验 + Δp 预测带）
- 模型（Alex 2026-07-25）：主循环 **Opus**、暂停 Fable——见 PROTOCOL §10。
  **iter 74 起 Opus 5 型号已可用，本轮实跑 = Claude Opus 5 (1M context)**，
  Alex 原话"用 opus 5"至此字面落地（§10 的"新型号出现即跟进"生效）。
- armed_at: **已拆（iter 84，2026-07-25）**——转按需模式，见 ROADMAP 末条。
  重建时按下方 cron 提示词逐字使用。
- cron_spec: `7,22,37,52 * * * *`
- 基点：master `4b32da4`（R1 起并入 `7ebdf6e` 修正关联式）；分支 `upgrade/loop`

## cron 提示词（重建定时器时逐字使用）

```
【升级循环】执行一轮 SJTU-TPMSHX 升级迭代。工作目录 E:\LWH\SJTU-TPMSHX-upgrade（若当前会话不在其中，用 EnterWorktree 的 path 参数进入）。严格按 upgrade/PROTOCOL.md 执行：先读 upgrade/STATE.md，恢复中断项或开始下一条 ROADMAP 条目，过验证门后本地提交并更新状态文件。一轮只做一项，绝不 push，绝不写主检出 E:\LWH\SJTU-TPMSHX。
```

## 环境备忘（详情见 PROTOCOL §2）

- venv 底座 C:\Python312（torch 装自 pytorch cpu 索引：`torch==2.11.0+cpu`）
- 测试唯一入口：`scripts\run_tests_server.ps1`
- 每轮开工断言：`data\raw_data\试验记录表_整理版.xlsx` 存在
