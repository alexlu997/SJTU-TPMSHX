# SJTU-TPMSHX runtime dependencies —— 三档依赖清单（2026-07-19 注）：
#   1) 本文件：宽松下界（>=），面向新环境安装与 CI。⚠ 不含 BO 优化器栈
#      （torch / botorch / gpytorch —— optimization/ 需要，历来由部署脚本按机器另装，
#      HANDOFF-windows-server.md §9e）；下方 torch 注释里的 "Optional/GPU" 描述已过时。
#   2) constraints-devbox-2026-07-11.txt：2026-07-11 开发机全量 freeze（187 行，== 精确，
#      含 torch+cu128 / botorch / tensorflow / ansys-fluent）。复现彼时 golden 的历史锚点：
#      pip install -r requirements.txt -c constraints-devbox-2026-07-11.txt
#   3) requirements-lock-server.txt：E:\LWH EPYC 服务器验证环境 80 包精确锁
#      （2026-07-19；全套 1245 绿 + golden 位同 + 上海双 gate；含完整 BO 栈）。
#      服务器复刻首选，用法见该文件头注。
#
# Tested on Windows 11 + Python 3.11/3.12. Versions reflect the development
# environment as of 2026-05-07; minor upgrades should be safe.

# --- Core numerical stack ---
numpy>=2.0
scipy>=1.13
pandas>=2.0
sympy>=1.13                 # MMS manufactured solutions (V&V)

# --- Linear solvers / ML surrogates ---
pyamg>=5.2                  # SPD AMG for 3D Pressure-Poisson + streamfunction
joblib>=1.3                 # surrogate model persistence
numba>=0.60                 # SIMPLE inner-kernel JIT
scikit-learn>=1.5           # D-F surrogate: Huber trend + GP residual (plhub_gp)

# --- Plotting / 3D rendering ---
matplotlib>=3.8
pyvista>=0.45
pyvistaqt>=0.11

# --- GUI ---
PySide6>=6.7

# --- IO ---
openpyxl>=3.1               # Excel (Shanghai validation data)

# --- Real-gas properties ---
CoolProp>=6.4               # Span-Wagner CO2 EOS for the sCO2 fluid (Phase A)

# --- Optional: GPU surrogate inference (commented; only needed for D-F training) ---
# torch>=2.2

# --- Test ---
pytest>=8.0
pytest-xdist>=3.5          # parallel test runs: pytest -n auto --dist loadscope
scikit-image>=0.24          # asym-porosity marching cubes (solvers/asym_geometry)
